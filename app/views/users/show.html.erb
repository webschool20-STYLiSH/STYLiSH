<%= stylesheet_link_tag  "SP_page/my_page", :media => "all" %>

<div class="container">
  <div class="row">
    <!-- プロフィール画像 -->
    <div class="col-xs-12 col-md-6">
      <div class="border image_profile background_color">
        <% if @user.image? %>
          <%= image_tag(@user.image.url, :size => "240x240") %>
        <% else %>
          <%= image_tag '画像がありません' %>
        <% end %>
      </div>
    </div>

    <div class="col-xs-12 col-md-6">
      <div class="row border">
        <!-- ユーザー名 -->
        <div class="col-xs-8 margin background_color text_center">
          <span class="user_name">
            <%= @user.name %>
          </span>
        </div>

        <!-- フォロー -->
        <div class="col-xs-4 margin background_color">
          <% if @user.id != current_user.id %>
            <% if @user.followed_by? current_user %>
              <%= link_to "フォローする", user_relationships_path(article.user), method: :delete, class: "follow not_follow" %>
            <% else %>
              <%= link_to "フォロー中", user_relationships_path(article.user), method: :article, class: "follow followed" %>
            <% end %>
          <% else %>
            <%= link_to "編集", edit_user_path(@user.id), class: "edit btn edit-btn" %>
          <% end %>
        </div>

        <div class="col-xs-12 background_color margin">
          <ul class="row user_info_li">
            <!-- 投稿数 -->
            <li class="col-xs-4 user_info_each">
              <%= link_to user_path(@user) do %>
                <div class="text">投稿数</div>
                <div class="num"><%= @user.articles.count %></div>
              <% end %>
            </li>

            <!-- フォロー数 -->
            <li class="col-xs-4 user_info_each">
              <%= link_to follows_user_path(@user) do %>
                <div class="text">フォロー</div>
                <div class="num"><%= @user.followings.count %></div>
              <% end %>
            </li>

            <!-- フォロワー数 -->
            <li class="col-xs-4 user_info_each">
              <%= link_to followers_user_path(@user) do %>
                <div class="text">フォロワー</div>
                <div class="num"><%= @user.followers.count %></div>
              <% end %>
            </li>

            <!-- いいね数 -->
            <li class="col-xs-4 user_info_each">
              <%= link_to favorites_user_path(@user) do %>
                <div class="text">いいね</div>
                <div class="num"><%= @user.favorites.count %></div>
              <% end %>
            </li>

            <!-- 性別 -->
            <li class="col-xs-4 user_info_each">
              <div class="text">性別</div>
              <div class="num"><%= @user.gender %></div>
            </li>

            <!-- 国 -->
            <li class="col-xs-4 user_info_each">
              <div class="text">国</div>
              <div class="num"><%= @user.country %></div>
            </li>
          </ul>
        </div>

        <!-- プロフィール -->
        <div class="col-xs-12 background_color text_profile">
          <%= @user.profile %>
        </div>
      </div>
    </div>
  </div>



<!-- 森山くんが作ってくれた部分、一応残しておく -->
 <!--  <div class="row">
    <% @user.articles.each do |article| %>
        <h4 class="user">
            <span class="user-name">
              <%= link_to article.user.email, user_path(article.user) %>
            </span>
        <span class="user-id"><%= article.user.email %></span>
        </h4>
        <div class="article-body">
            <%= article.body %>
        </div>
    <% end %> -->


    <%# @articles.each do |article| %>
      <%#= link_to article.title, article_path(article.id) %>
      <%# if article.image.present? %>
      	<%#= image_tag article.image.url %>
      <%# end %>
      <!-- <%#= article.body %> -->
    <%# end %>

  <div class="row">
    <% @articles.each do |article| %>
      <ul class="margin col-lg-4 col-md-6 col-xs-12">
        <li class="border">
          <article class="background_color">

            <!-- タイトル -->
            <div class="title">
              <%= link_to article.title, article_path(article.id) %>
            </div>

            <!-- 画像 -->
            <div class="image_article">
              <% if article.image? %>
                <%= image_tag article.image.url %>
              <% else %>
                <p>画像がありません</p>
              <% end %>
            </div>

            <!-- タグ -->
            <%= render 'articles/tag_list', tag_list: article.interest_list %>


            <!-- いいね -->
            <% if article.favorited_by? current_user %>
              <%= link_to image_tag('fav.png', {:width => '25', :height => '25'}), article_favorites_path(article), method: :delete %>
              <%#= link_to "いいね済", article_favorites_path(article), method: :delete %>
            <% else %>
              <%= link_to image_tag('un_fav.png', {:width => '25', :height => '25'}), article_favorites_path(article), method: :article %>
            <% end %>
            <!-- 何個いいねがあるかもあとで書き足したい -->

            <!-- ここらへんにコメント欄を作成予定 -->

          </article>
        </li>
      </ul>
    <% end %>
  </div>


  </div>
</div>