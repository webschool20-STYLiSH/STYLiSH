<%= stylesheet_link_tag  "css/articles/index", :media => "all" %>

<div class="container">
	<div class="row">
		<div class="col-xs-12 border background_color box_main">
			<%= link_to "コーデを追加", new_article_path, class: "text" %>
		</div>
	</div>
	<div class="row">
		<% @articles.each do |article| %>
			<%= render 'articles/article', article: article  %>
		<% end %>
	</div>
</div>

<%= paginate @articles %>
