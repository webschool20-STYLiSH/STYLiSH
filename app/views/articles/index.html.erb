<%= link_to "新しいコーディネイト" ,new_article_path, class: "btn" %>
<h1>投稿一覧</h1>
<% @articles.each do |article| %>
	<p>タイトル</p>
	<span>
		<%=link_to article.title, article_path(article.id) %>
	</span>
	<p>image</p>
	<% if article.image? %>
		<%= image_tag article.image.url %>
		<% else %>
		<p>画像がありません</p>
	<% end %>
<% end %>